angular
    .module('app.content');

/*
angular
    .module('app.content')
    .config(['$routeProvider', function($routeProvider) {
        $routeProvider
            .when('/display/list', {
                templateUrl: 'content/psw-list.html'
            })
            .otherwise({
                redirectTo: '/display/list'
            });
    }]);
*/